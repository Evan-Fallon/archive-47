---
import { getVar } from '../utils/ingest';
const data = await getVar()
---
<style is:global> 
body, h1 {
    background: radial-gradient(hsl(0 0 10), hsl(0 0 2)) !important;
    max-width: 100%;
    width: 100%;
    height: 100vh;
    font-family: Noto Sans !important;
    display: grid;
    grid-template-columns: 20px minmax(1fr 720px) auto;
    gap: 50px;
    > * {
        grid-column: 2 / 3;
    }
} 
@font_face {
    font-family: "us-election-main";
    src: url("/fonts/NotoSans-Bold.ttf") format("truetype");
}
</style>
<a href="./The CECOT Lie"> The CECOT Lie</a>